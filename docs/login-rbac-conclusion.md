# 登录与 RBAC 实现结论

## 已完成范围

- 全站登录门禁接入
- 登录页调整为全站唯一入口
- `admin` / `member` 角色分流完成
- `admin` 后台访问隔离完成
- `member` 后台访问拦截完成

## 后端已实现

- `users` 模型补充 `bio`、`last_login_at`
- 登录成功写入 `last_login_at`
- 当前用户资料查询接口保留
- 当前用户资料修改接口完成
- 当前用户密码修改接口完成
- 管理员用户管理接口完成
- 本人文章 CRUD 接口完成
- 文章作者摘要返回完成
- 文章权限判断下沉到 service
- 前台文章接口切换为登录后访问

## 前端已实现

- 登录态恢复与失效回跳流程完成
- 全站未登录跳转 `/login`
- 登录成功默认进入 `#/`
- 顶部导航改为登录后用户态导航
- 个人中心页面完成
- 个人资料编辑页面完成
- 个人密码修改页面完成
- 我的文章列表页面完成
- 我的文章新建/编辑页面完成
- 管理员后台文章管理保留
- 管理员后台用户管理页面完成

## 结果状态

- 站点已从“后台独立登录”切换为“全站登录后访问”
- `admin` 与 `member` 的功能视图已按要求分离
- `member` 已具备本人内容与本人资料操作能力
